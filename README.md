# Azure AI Search (Vector DB)
azure-search-documents==11.6.0
azure-core==1.30.2

# Azure OpenAI SDK (new SDK you are using)
openai==1.52.0

# Excel handling
openpyxl==3.1.5

# YAML user story input
pyyaml==6.0.2

# HTML cleaning for channel detection
beautifulsoup4==4.12.3

# Optional but recommended (parsing, utilities)
python-dotenv==1.0.1
